{
  "git": {
    "changelog": "npx auto-changelog --stdout --commit-limit false -u --ignore-commit-pattern=yarn.lock"
  },
  "hooks": {
    "after:bump": "npx auto-changelog -p --ignore-commit-pattern=yarn.lock"
  }
}
